# Greenhouse AI — Edge-Central Hybrid Decision Support System

An edge-central hybrid decision support system for smart greenhouse management.
Each greenhouse node runs a set of specialist ML models (disease detection,
irrigation, nutrition, anomaly) locally and remains fully autonomous during
cloud disconnections. A central layer aggregates multi-node data for yield
forecasting and LLM-powered alarm explanations.

---

## Architecture

```
┌─────────────────────────────────────────────┐
│              EDGE LAYER                     │
│         (1 node per greenhouse)             │
│                                             │
│  Simulator → [disease, irrigation,          │
│               nutrition, anomaly]           │
│            → Model Output Monitor           │
│            → Fusion / Rule Engine           │
│            → Alarm Engine                  │
│            → Local Grafana + SQLite         │
│            → Edge Gateway (FastAPI)         │
└──────────────────┬──────────────────────────┘
                   │ HTTP sync
┌──────────────────▼──────────────────────────┐
│             CENTRAL LAYER                   │
│                                             │
│  Aggregator → LSTM Yield → LLM Explainer   │
│  Central Gateway → Central Grafana          │
│  Central SQLite                             │
└─────────────────────────────────────────────┘
```

Design principles: Specialist ML per task · Edge-first autonomous operation ·
Deterministic rule engine decides, LLM explains · Config-over-code · Contract-first APIs.

---

## Quick Start

```bash
# 1. Clone the repository
git clone <repo-url>
cd greenhouse-ai

# 2. Configure environment (set LLM_API_KEY for real LLM explanations)
cp .env.example .env

# 3. Start full system and wait for all services to become healthy
./scripts/start_demo.sh
```

All services healthy in under 10 minutes. No manual steps.

---

## Demo Scenarios

```bash
# Run all 5 demo scenarios
python tests/demo/inject_scenario.py --scenario all --node 1

# Individual scenarios
python tests/demo/inject_scenario.py --scenario normal   --node 1
python tests/demo/inject_scenario.py --scenario fungal   --node 1
python tests/demo/inject_scenario.py --scenario dry      --node 1
python tests/demo/inject_scenario.py --scenario anomaly  --node 1
python tests/demo/inject_scenario.py --scenario drift    --node 1

# List scenarios
python tests/demo/inject_scenario.py --list
```

---

## KPI Measurement

```bash
python tests/demo/measure_kpi.py
# Generates KPI_REPORT.md with all measurable KPIs vs targets
```

| KPI | Target | Description |
|-----|--------|-------------|
| Alarm latency | ≤ 2000ms | POST /predict → alarm returned |
| LLM latency | ≤ 5000ms | POST /explain → explanation returned |
| Monitor precision | ≥ 0.85 | Injected drift detection |
| Anomaly precision | ≥ 0.88 | Isolation Forest on extreme values |

---

## Service URLs

| Service | URL | Description |
|---------|-----|-------------|
| Edge 1 Gateway | http://localhost:8100 | Node 1 main API |
| Edge 1 Disease | http://localhost:8101 | ResNet-50 disease detection |
| Edge 1 Irrigation | http://localhost:8102 | XGBoost irrigation |
| Edge 1 Nutrition | http://localhost:8103 | Random Forest nutrition |
| Edge 1 Anomaly | http://localhost:8104 | Isolation Forest anomaly |
| Edge 1 Monitor | http://localhost:8105 | Z-score model output monitor |
| Edge 1 Fusion | http://localhost:8106 | Rule fusion engine |
| Edge 1 Alarm | http://localhost:8107 | Alarm engine |
| Edge 2 Gateway | http://localhost:8200 | Node 2 main API |
| Central Gateway | http://localhost:9000 | Central API |
| Central Aggregator | http://localhost:9001 | Multi-node aggregator |
| Central LSTM | http://localhost:9002 | Yield forecast |
| Central LLM | http://localhost:9003 | LLM alarm explainer |
| Central Grafana | http://localhost:3000 | Multi-node dashboard |
| Edge 1 Grafana | http://localhost:3001 | Node 1 local dashboard |
| Edge 2 Grafana | http://localhost:3002 | Node 2 local dashboard |

---

## Directory Structure

```
greenhouse-ai/
├── README.md
├── docker-compose.yml
├── .env.example
├── KPI_REPORT.md           ← generated by measure_kpi.py
├── scripts/
│   ├── start_demo.sh       ← one-command startup
│   ├── wait_for_services.sh
│   └── health_check.sh
├── docs/
│   ├── DEMO_GUIDE.md
│   ├── DEPLOYMENT.md
│   ├── architecture.md
│   └── api-reference.md
├── simulator/
│   ├── sensor_simulator.py
│   ├── image_simulator.py
│   └── config/             ← node1.yaml, node2.yaml
├── models/
│   ├── disease/            (ResNet-50,       port 8101)
│   ├── irrigation/         (XGBoost,         port 8102)
│   ├── nutrition/          (RandomForest,    port 8103)
│   ├── anomaly/            (IsolationForest, port 8104)
│   └── output_monitor/     (Z-score,         port 8105)
├── edge/
│   ├── fusion_engine/      (port 8106)
│   ├── alarm_engine/       (port 8107)
│   └── gateway/            (port 8100)
├── central/
│   ├── aggregator/         (port 9001)
│   ├── lstm_yield/         (port 9002)
│   ├── llm_explainer/      (port 9003)
│   └── gateway/            (port 9000)
├── database/
│   ├── schema.sql
│   └── init.py
├── monitoring/
│   └── grafana/            ← Dockerfile + provisioning + dashboards
└── tests/
    ├── unit/
    ├── integration/
    └── demo/
```

---

## Technology Stack

| Layer | Technology |
|-------|-----------|
| Disease detection | ResNet-50 (PyTorch, transfer learning) |
| Irrigation decision | XGBoost classification + regression |
| Nutrition analysis | Random Forest multi-class |
| Anomaly detection | Isolation Forest |
| Model monitoring | Z-score + IQR sliding window |
| Yield forecasting | 2-layer LSTM (PyTorch) |
| LLM explanations | OpenAI-compatible API (gpt-4o-mini) |
| Decision engine | Deterministic YAML rule engine (10 rules) |
| API framework | FastAPI + uvicorn (Python 3.11) |
| Data storage | SQLite (edge: greenhouse.db, central: central.db) |
| Monitoring | Grafana 10.4 + frser-sqlite-datasource |
| Orchestration | Docker Compose |

---

## Testing

```bash
# Integration tests (all services must be running)
pytest tests/integration/test_full_system.py -v --tb=short

# All existing integration tests
pytest tests/integration/ -v

# Unit tests
pytest tests/unit/ -v

# Disconnection test
python tests/demo/test_disconnection.py

# Health check
./scripts/health_check.sh
```

---

## Documentation

- [Demo Guide](docs/DEMO_GUIDE.md) — step-by-step demo script for presentations
- [Deployment Guide](docs/DEPLOYMENT.md) — installation, configuration, troubleshooting
- [Architecture](docs/architecture.md)
- [API Reference](docs/api-reference.md)

---

## Team

_Placeholder — add team members here._
